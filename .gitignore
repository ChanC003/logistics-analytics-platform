# Generated data — large parquet files, do not commit
data/raw/
data/mart/
data/warehouse.duckdb
data/warehouse.duckdb.wal

# Sample data — OK to commit (50k rows, small)
!data/sample/

# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# dbt
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/
dbt_project/.user.yml

# Airflow
logs/
airflow.db
airflow.cfg

# Secrets
.env
*.env

# Metabase — driver JARs are large binaries (285MB+), download separately (see docs/setup.md)
metabase/plugins/
# Metabase runtime DB
metabase/metabase.db*
metabase/metabase.db.mv.db

# Internal docs — only setup.md + architecture.md + screenshots are public
docs/data-model.md
docs/data-quality-strategy.md
docs/entity-map.md
docs/learning-guide.md
.claude/
CLAUDE.md
processing.md

# Metabase dashboard runtime DB (stored in dashboards/ subfolder)
metabase/dashboards/metabase.db*

# Render demo — exported CSVs (regenerate with scripts/export_mart_to_csv.py)
scripts/mart_export/

# OS
.DS_Store
Thumbs.db
